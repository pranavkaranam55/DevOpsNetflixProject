apiVersion: v1
kind: Service
metadata:
  name: netflix-service
spec:
  type: NodePort
  selector:
    app: netflix
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30007